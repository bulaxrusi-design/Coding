name: Build xuci1.0 Alpha APK

on:
  push:
    branches: [xuci-alpha-build]
    paths:
      - 'xuci1.0-alpha/**'
      - '.github/workflows/xuci-alpha-build.yml'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Java 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'

      - name: Set up Gradle 8.13
        uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: '8.13'

      - name: Set up Android SDK
        uses: android-actions/setup-android@v3

      - name: Install Android platform
        run: yes | sdkmanager 'platforms;android-36' 'build-tools;35.0.0' 'platform-tools'

      - name: Build debug APK
        working-directory: xuci1.0-alpha
        run: gradle --no-daemon --stacktrace :app:assembleDebug

      - name: Verify artifact
        run: |
          test -f xuci1.0-alpha/app/build/outputs/apk/debug/app-debug.apk
          sha256sum xuci1.0-alpha/app/build/outputs/apk/debug/app-debug.apk

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: xuci1.0-alpha-debug-apk
          path: xuci1.0-alpha/app/build/outputs/apk/debug/app-debug.apk
          if-no-files-found: error
          retention-days: 14
